# ── Unified dependencies for VoxHub ──────────────────
pyannote.audio
ffmpeg-python
pydantic
rich
pyyaml
mistral-common
numpy
soundfile
transformers>=4.49.0
accelerate
peft  # Required by ibm-granite/granite-4.0-1b-speech (bundled LoRA adapter)
fastsafetensors>=0.1.10
silero-vad
# Whisper now uses transformers (no faster-whisper / CTranslate2 needed)
torchao
bitsandbytes
compressed-tensors
# nemo_toolkit[asr] -- handled in Dockerfile or specialized env

# ── API dependencies ───────────────────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.34.0
python-multipart
pydantic-settings

# ── Optional: vLLM backend client ──────────────────────────────
# Used by core/transcribe_voxtral_vllm.py to talk to a remote vLLM
# server via the OpenAI-compatible /v1/audio/transcriptions endpoint.
# The vLLM server itself is NOT installed here — it runs as a
# separate container (see `voxtral-vllm` service in docker-compose.yaml).
openai>=1.0
