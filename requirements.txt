# ── Unified dependencies for VoxHub ──────────────────
# Pin torch+torchaudio together to avoid ABI mismatches when resolved
# independently by different packages (pyannote, silero-vad, etc.)
torch>=2.4.0,<2.8
torchaudio>=2.4.0,<2.8
pyannote.audio
ffmpeg-python
pydantic
rich
pyyaml
mistral-common
numpy
soundfile
transformers>=4.49.0
accelerate
fastsafetensors>=0.1.10
silero-vad
# Whisper now uses transformers (no faster-whisper / CTranslate2 needed)
torchao
bitsandbytes
compressed-tensors
# nemo_toolkit[asr] -- handled in Dockerfile or specialized env

# ── API dependencies ───────────────────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.34.0
python-multipart
pydantic-settings
